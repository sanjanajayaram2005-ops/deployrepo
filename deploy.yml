- hosts: localhost
  become: yes
  tasks:
    - name: Copy WAR file to Tomcat webapps directory
      copy:
        src: target/maven-webapp-final.war
        dest: /opt/tomcat/webapps/maven-webapp-final.war

